<?xml version="1.0" encoding="utf-8"?>
<!--
  Merge these nodes into your real AndroidManifest.xml inside <manifest> and <application>.

  Replace .service.RaktaFcmService with your applicationId + path if different.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network (Firebase, FCM) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- FCM: show notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional: wake device briefly for time-sensitive delivery (use sparingly) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Firebase Storage: reading photos from gallery (API-dependent; add if you pick images) -->
    <!-- <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> -->
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" /> -->

    <application>

        <!-- Default FCM notification icon & color (optional but recommended) -->
        <!--
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />
        -->

        <service
            android:name=".service.RaktaFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
