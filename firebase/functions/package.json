{
  "name": "raktaseva-connect-functions",
  "private": true,
  "description": "Send high-priority FCM when a BLOOD_REQUEST notification document is created",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint --ext .js,.ts ."
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
